pipeline {
    agent any 
    stages {
        stage('Stage 1') {
            steps {
                sh """ssh -tt login@host << EOF
                sudo apt upgarde
                exit
                EOF"""
            }
        }
    }
}